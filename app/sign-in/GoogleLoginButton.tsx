"use client";

import GoogleButton from "react-google-button";

const GoogleLoginButton = () => {
  return (
    <button>
      <GoogleButton />
    </button>
  );
};

export default GoogleLoginButton;
